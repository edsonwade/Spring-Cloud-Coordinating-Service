version: '3'

spring:
  application:
    name: tollrate-service-ui

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
  instance:
    hostname: localhost

management:
  endpoints:
    web:
      exposure:
        include: '*'



server:
  port: 8081
